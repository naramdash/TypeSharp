# Parser Fixture Format

문서 기준일: 2026-05-20

이 파일은 task `0251-docs-site-canonical-language-ledger` 이후 남아 있는 short bridge stub다.

Canonical parser fixture layout, snapshot, diagnostic JSON, and current parser coverage policy는 [docs-site Project Policy](../docs-site/src/content/docs/project-policy.md)의 `Parser Fixture Policy` 섹션이다.

## Bridge Scope

- Parser fixture directory layout, positive/negative fixture rules, diagnostic JSON shape, syntax tree snapshot policy, test runner contract, and current stable parser fixture coverage summary는 docs-site Project Policy로 접혔다.
- Diagnostic JSON rules are in [docs-site Diagnostics](../docs-site/src/content/docs/diagnostics.md).
- 이 bridge는 active agent가 예전 경로를 열었을 때 target canonical pages를 찾을 수 있게 하기 위해서만 남아 있다.
