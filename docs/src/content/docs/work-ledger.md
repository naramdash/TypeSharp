---
title: Work Ledger
description: Current long-running TypeSharp task state and completed work rollup.
---

This page is the web-facing view of the long-running task ledger. The compact source record lives in `agent/tasks.md`, including user and agent task sections, the active task packet, and `agent/tasks-rollup.md`.

Codex CLI goal and other long-running agents should still read the temporary operational files under `agent/` when selecting work. This page exists so humans can inspect the same state through the docs site.

## Current State

| Item | State |
| --- | --- |
| Active task packet | [`0441-imported-csharp-indexer-multiplicative-compound-assignment-targets.md`](https://github.com/naramdash/TypeSharp/blob/main/agent/0441-imported-csharp-indexer-multiplicative-compound-assignment-targets.md) |
| Next top-priority task | Task 0441 imported C# indexer multiplicative compound assignment targets. |
| Task queue owner | [`agent/tasks.md`](https://github.com/naramdash/TypeSharp/blob/main/agent/tasks.md) |
| Current tooling slice | Task 0440 rechecked official language/platform/package/tooling signals after imported C# member multiplicative compound assignment targets, reaffirmed the package-free generated `net48`/C# 7.3 baseline and the isolated `net10.0` MSTest.Sdk/MTP package bridge, kept Task 0401 blocked without explicit approval, and selected imported C# indexer `*=`, `/=`, and `%=` targets as Task 0441. |
| Completed work covered | 0001-0400, 0402-0440 |
| Canonical task index | [`agent/tasks.md`](https://github.com/naramdash/TypeSharp/blob/main/agent/tasks.md) |
| Compressed work ledger | [`agent/tasks-rollup.md`](https://github.com/naramdash/TypeSharp/blob/main/agent/tasks-rollup.md) |

## Completed Work Themes

| Theme | Covered Work |
| --- | --- |
| Foundation, parser, semantics | Fixture policy, grammar precedence/ambiguity, compiler/CLI skeleton, parser coverage, diagnostics, binder, type checker, duplicate symbol diagnostics. |
| Runtime, backend, lowering | Runtime/core helpers, generated `net48` C# build pipeline, runtime artifact architecture docs with Mermaid diagrams, public API lowering, records, simple enum declarations including explicit underlying types, explicit numeric member values, numeric range validation, explicit member aliases, enum declaration/member attributes, enum composite member expressions, expression-level same-enum value `|`/`&`/`^` plus unary `~` expressions, integral numeric `|`/`&`/`^` plus unary `~` and bounded `<<`/`>>`/`>>>` shift expressions, boolean `|`/`&`/`^` expressions over known non-null `bool` operands, local multiplicative compound assignment `*=`/`/=`/`%=`, imported C# instance/static field/property member multiplicative compound assignment `*=`/`/=`/`%=`, bitwise compound assignment `|=`/`&=`/`^=`, shift assignment `<<=`/`>>=`, local plus metadata-backed imported C# field/property/indexer logical unsigned shift assignment `>>>=` with C# 7.3-compatible explicit assignment/cast lowering plus single-evaluation receiver and index-argument behavior for non-trivial imported targets, metadata-backed imported C# instance field/property null-conditional assignment `receiver?.Member = value`, metadata-backed imported C# instance indexer null-conditional assignment `receiver?[index] = value`, metadata-backed imported C# instance field/property/indexer null-conditional reads `receiver?.Member` and `receiver?[index]`, null-conditional logical unsigned shift assignment `receiver?.Member >>>= count` and `receiver?[index] >>>= count`, null-conditional member/indexer shift compound assignment `receiver?.Member <<= count`, `receiver?.Member >>= count`, `receiver?[index] <<= count`, and `receiver?[index] >>= count`, null-conditional member/indexer additive compound assignment `receiver?.Member += value`, `receiver?.Member -= value`, `receiver?[index] += value`, and `receiver?[index] -= value`, and null-conditional member/indexer bitwise compound assignment `receiver?.Member |= value`, `receiver?.Member &= value`, `receiver?.Member ^= value`, `receiver?[index] |= value`, `receiver?[index] &= value`, and `receiver?[index] ^= value` with explicit C# 7.3 `System.Func` null/index guards, skipped index/right-side/count evaluation on null assignment receivers, nullable C# result types for value-type reads, and single receiver evaluation, local `let mut` assignment target diagnostics, direct TypeSharp-declared function call arity/argument diagnostics, TypeSharp-owned final-array `params` parameter declarations with exact/expanded direct-call and pipeline validation plus C# `params T[]` lowering, TypeSharp-owned trailing literal default parameter declarations with omitted direct-call and first-argument pipeline validation plus C# optional-parameter metadata lowering, TypeSharp-owned named argument binding for non-generic and bounded generic direct calls plus named non-piped pipeline arguments with positional C# lowering, direct TypeSharp generic function call inference for simple, params-tail, and bounded constructed type-parameter parameter/return positions, invalid value-shaped `>>`/`<<` shift diagnostics while preserving function-shaped composition and invalid `>>>` operand diagnostics before backend emission, direct named-function composition compatibility diagnostics including direct TypeSharp generic unary composition inference/substitution, explicit composition function-type annotation compatibility, private/internal direct composition value delegate inference, and public unannotated direct composition annotation diagnostics, direct pipeline input/arity/call-argument compatibility diagnostics including direct TypeSharp generic pipeline inference/substitution, unions, nominal/TypeSharp-owned enum/imported C# enum/type-level union match exhaustiveness with discard arms and guarded arm non-coverage, bool and local literal-union match exhaustiveness/lowering, nominal union discard fallback lowering, match `when` guard lowering, null safety, structural checks, structural intersection aliases, limited `keyof`, limited indexed access types, and `satisfies` proof erasure, finite evaluator budget design for future advanced type operators, async `Task`, iterator `yield`, block-level `lock`, explicit-receiver extension methods and getter-only TypeSharp-authored extension properties with duplicate/precedence/helper-name/nullable receiver declaration/access/assignment diagnostics plus C# 7.3 helper-method lowering, collection expressions including array/List spread lowering, pipeline/composition/indexer/parenthesized/unary logical-not/unary numeric sign/value-producing `if` expression/block-bodied lambdas/collection-return lambda bodies/`nameof` including unbound generic string lowering/checked-unchecked/record expressions including nominal record spread, partial declarations, explicit and inferred generic C# calls. |
| C# interop | Framework/local DLL references, framework/local metadata reader including enum member, underlying type, and literal numeric metadata, constructor parameter validation including generic constructor type substitution, optional/named/params constructor calls and ambiguous constructor diagnostics, overload/byref/params/optional/named validation including known literal/null/imported metadata argument type, direct unary numeric arguments, homogeneous collection expression array arguments, single collection expression params-array arguments, and parenthesized overload argument unwrapping, delegate lambda arity, known delegate lambda return type, identity lambda parameter return filtering, known lambda return conversion ranking, metadata-backed lambda member-chain return filtering, metadata-backed lambda method-call return filtering, metadata-backed lambda extension method-call return filtering, metadata-backed lambda static method-call return filtering, metadata-backed lambda binary predicate return filtering, metadata-backed lambda binary value return filtering, lambda `nameof` return filtering, checked/unchecked lambda return filtering, `satisfies` proof-erasure lambda return filtering, parenthesized lambda return filtering, unary logical-not lambda return filtering, unary numeric lambda return filtering, `if` expression lambda return filtering, block lambda final-expression return filtering, collection expression lambda return filtering, metadata-backed lambda null-coalescing return filtering, and metadata-backed lambda indexer-expression return filtering, imported C# enum exhaustiveness, null metadata-specificity ranking, method/indexer/extension receiver metadata relationship distance, extension receiver `object` fallback, and numeric literal conversion filtering, explicit and inferred generic method constraint validation including constructed generic arguments, homogeneous collection expression `T[]` arguments, framework constraints, and transitive base/interface metadata checks, imported C# class-to-interface/base assignment validation, imported indexer parameter type, null literal indexer filtering/ranking, parenthesized and unary numeric indexer argument validation, numeric literal conversion, exact/metadata-relationship ranking, and ambiguous candidate validation, extension method metadata markers, imported receiver instance-call syntax and extension no-matching overload diagnostics, imported C# field/property/indexer `>>>=` checker/backend lowering with non-trivial receiver/index-argument single-evaluation evidence, imported C# instance/static field/property member `*=`, `/=`, and `%=` checker/backend lowering with ordinary C# 7.3 compound-assignment output and single receiver emission, imported C# instance field/property and indexer null-conditional assignment checker/backend lowering with skipped RHS/index evaluation on null receivers, imported C# instance field/property/indexer null-conditional read checker/backend lowering with nullable-compatible value-type results, imported C# instance field/property/indexer null-conditional `>>>=` checker/backend lowering with skipped count/index evaluation on null receivers, imported C# instance field/property/indexer null-conditional `<<=`/`>>=` checker/backend lowering with skipped count/index evaluation on null receivers, imported C# instance field/property/indexer null-conditional `+=`/`-=` checker/backend lowering with skipped RHS/index evaluation on null receivers, imported C# instance field/property/indexer null-conditional `|=`/`&=`/`^=` checker/backend lowering with skipped RHS/index evaluation on null receivers, nullable diagnostics, delegates/events, fields, generic methods, generic type constructor calls, interfaces, attributes, unsupported package references, framework-backed missing type/method/member diagnostics, metadata-backed `TS2402`/`TS2406`-`TS2417` diagnostics, imported C# parameter/local alias/assignment receiver tracking, and Microsoft Learn C#-style detailed docs for CLR type mapping, member lookup, overloads, delegates, events, extension methods, and diagnostics. |
| C# interop update | Task 0421 added imported C# instance field/property null-conditional logical unsigned shift assignment checker/backend lowering for readable/writable metadata-backed instance member targets, reusing the existing primitive target/count policy, evaluating the receiver once, skipping `count` on null receivers, emitting C# 7.3-compatible null guards plus assignment/cast shapes, and preserving deterministic diagnostics for readonly, event, static, indexer, invalid-count, and other compound targets. Task 0423 added the paired imported C# instance indexer slice `receiver?[index] >>>= count`, reusing indexer argument validation/ranking, skipping index arguments and `count` on null receivers, and preserving the same generated `net48` and test-host package boundaries. Task 0425 added imported C# instance field/property null-conditional bitwise compound assignment `receiver?.Member |= value`, `receiver?.Member &= value`, and `receiver?.Member ^= value`, reusing the existing primitive integral/enum/bool bitwise compound policy, skipping the right side on null receivers, and preserving deterministic diagnostics for unsupported operands, readonly, event, and static targets. Task 0427 added the paired imported C# instance indexer slice `receiver?[index] |= value`, `receiver?[index] &= value`, and `receiver?[index] ^= value`, reusing indexer argument validation/ranking and the same bitwise target/value policy, skipping index arguments and the right side on null receivers, and preserving the generated `net48` and test-host package boundaries. Task 0429 added imported C# instance field/property null-conditional additive compound assignment `receiver?.Member += value` and `receiver?.Member -= value`, using a bounded primitive integral target/value policy, skipping the right side on null receivers, and preserving deterministic diagnostics for unsupported operands, narrowing results, readonly, event, static, indexer, and TypeSharp-owned targets. Task 0431 added the paired imported C# instance indexer slice `receiver?[index] += value` and `receiver?[index] -= value`, reusing indexer argument validation/ranking and the same additive primitive integral policy, skipping index arguments and the right side on null receivers, and preserving the generated `net48` and test-host package boundaries. Task 0433 added imported C# instance field/property null-conditional shift compound assignment `receiver?.Member <<= count` and `receiver?.Member >>= count`, reusing the existing primitive integral target/count policy, skipping `count` on null receivers, preserving single receiver evaluation. Task 0434 rechecked official signals and selected the paired null-conditional indexer `<<=`/`>>=` slice as Task 0435. Task 0435 added that paired imported C# instance indexer slice `receiver?[index] <<= count` and `receiver?[index] >>= count`, reusing indexer argument validation/ranking and the regular shift target/count policy, skipping index arguments and `count` on null receivers, and preserving the generated `net48` and test-host package boundaries. Task 0436 rechecked official signals again and selected local multiplicative compound assignment as the next foundation slice before imported or null-conditional multiplicative targets. Task 0438 rechecked official signals after that local foundation and selected imported C# member `*=`, `/=`, and `%=` targets as the next bounded interop slice before indexer or null-conditional multiplicative expansion. Task 0439 added those imported C# instance/static field/property member multiplicative compound assignment targets, reusing the primitive integral assign-back policy and preserving ordinary C# 7.3 compound-assignment lowering with the receiver emitted once. Task 0440 rechecked official signals again and selected imported C# indexer `*=`, `/=`, and `%=` targets as the next bounded interop slice before null-conditional multiplicative expansion. |
| CLI and VS Code | `check`, `build`, `run`, `format`, `new`, `lsp`, common options, warning gates, target/configuration/verbosity handling, strict option parsing, semantic manifest value validation, future `typesharp restore` package-graph separation, VS Code activation, TextMate syntax highlighting for `.tysh`, shared docs highlighting grammar reuse, local VSIX packaging/install docs, temporary Marketplace publishing guide, CLI/LSP parity roadmap, LSP document open/change/close synchronization, stale diagnostic clearing, diagnostics, hover, definition, completion, and formatting. |
| Docs and adoption | Astro Starlight docs site, TypeScript docs configuration, `tysh` syntax-highlighted TypeSharp examples through the shared TextMate grammar, Mermaid-rendered architecture diagrams, TypeSharp docs writing guide with `tysh` example project and emoji policy, Microsoft Learn C#-style detailed reference pages, GitHub Pages workflow, GitHub Release artifact workflow for CLI/runtime/VSIX/checksum assets, NuGet restore/security and `net48`/`net481` profile policy, pinned current docs package dependencies, realistic smoke-tested runnable examples with per-code-block README explanations, migration guide, docs benchmarks, host compatibility notes, progress/ADR/regression policy, release readiness, and traceability. |
| Repository layout | Task 0270 split the monorepo into explicit top-level ownership folders: `cli/` for the CLI host, `lang/` for compiler/LSP/runtime/core projects, `test/` for the regression suite and fixtures, `docs/` for the public Starlight site, `agent/` for temporary operational records, `examples/` for runnable artifacts, and `vscode/` for editor integration. Each top-level ownership folder now has a README contract and a layout regression test. |
| Agentic bootstrap | Post-0257 bootstrap docs now direct agents to docs canonical standard ledger pages, keep `agent/` as the temporary work surface, require task-end commit/push handoff in `agent.md`, keep the public docs source under `docs/`, keep `User Task Inbox` user-editable at any time, limit the visible `Agent Task Queue` to the latest five rows, and require roadmap-refresh planning when the queue/checklist would otherwise appear empty. |
| Source modules and safety gates | Capability diagnostics, `unknown` narrowing, root namespace fallback, ambient signatures, `open`, import aliases, source module path identity, relative source graph/lowering, multi-source containers, export parsing, local export-list public surface, local/relative named function import/export/re-export alias forwarding, local literal export alias forwarding, local top-level value export alias lowering, explicitly annotated and conservatively inferred lambda-valued top-level `let` export and alias lowering with block and collection return bodies, relative top-level value import/re-export alias lowering, local/relative type import/export alias lowering including regular named exported type aliases, relative named module import alias lowering, relative named module re-export alias remapping, relative type-only re-export remapping, relative star re-exports over the currently lowerable function/value/type surface, current-project manifest-owned source alias parsing/validation/resolution for imports and function/value/type/module/star re-export barrels, direct TypeSharp project-reference manifest loading, graph diagnostics, direct source imports, referenced-project build ordering, generated assembly consumption, missing project export diagnostics, hidden transitive source import rejection, and project-reference re-export backlog boundaries. |
| Extension member hardening | Task 0407 added same-container helper-name collision diagnostics for getter-only TypeSharp-authored extension property helpers such as `GetWordCount`, covering collisions with TypeSharp-authored extension methods and defensive generated-helper collisions before backend emission. Task 0408 kept the same generated-artifact and test-host baseline and selected nullable receiver declaration diagnostics as the next bounded hardening slice. Task 0409 added those nullable receiver diagnostics before nullable receiver lifting expands the surface. Task 0410 kept the same baseline and selected getter-only extension property assignment diagnostics. Task 0411 added those assignment diagnostics before setters expand the surface. Task 0412 kept the baseline unchanged and selected nullable receiver access diagnostics. Task 0413 added those access diagnostics before nullable receiver lifting expands the surface. Task 0414 kept the baseline unchanged and selected extension-property-specific null-conditional access diagnostics. Task 0415 added those `?.` read/simple assignment diagnostics before TypeSharp-owned null-conditional extension-property lowering expands. |
| Regression quality | Task 0256 audited parser/backend/diagnostic fixtures, CLI/run/example smokes, metadata/C# interop, runtime/core, VS Code mocked/live/host tests, docs build smoke, and generated residue handling; it hardened fixture README coverage, diagnostic polarity, runnable CLI stderr checks, and Extension Host exit semantics. Task 0368 kept the main full runner as the release-confidence gate while adding process-safe cached legacy reference assemblies and four parallel shard projects that ran the then-full 517-test catalog in about 68 seconds. Task 0370 extracted `TypeSharpCompilerTestCases.All`, kept the custom main/shard runners on the same catalog, and added a pinned `MSTest.Sdk/4.2.3` `dotnet test` bridge over the then-517-case catalog. Task 0372 selected the existing MSTest SDK/MTP bridge over adding xUnit.net v3 now, then added test-host-only NuGet source mapping, audit, repo-local package cache, and lock-file controls. Task 0374 added a Windows regression workflow that runs the package-free shard runner and focused MSTest bridge smoke. Task 0375 rechecked official signals after the CI gate and confirmed no generated-artifact baseline change. Task 0376 added optional/default parameter parser, type-checker, backend, generated `net48`, C# consumer, and MSTest catalog-count evidence, bringing the shared catalog to 518 cases. Task 0377 rechecked official signals after optional/default parameters, kept the generated-artifact baseline unchanged, and selected direct TypeSharp named argument binding next. Task 0378 added named-argument checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 519 cases. Task 0380 added generic named-argument checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 520 cases. Task 0381 rechecked official signals after generic named arguments, confirmed `MSTest.Sdk/4.2.3` is already the selected `net10.0` package bridge, and selected package-based MSTest shard execution next. Task 0382 added four locked MSTest SDK/MTP shard projects over the same catalog, updated CI to run package-based shards in parallel, and brought the shared catalog to 521 cases. Task 0383 rechecked official signals after MSTest package sharding, kept the generated-artifact baseline unchanged, and selected generic optional/default parameters next. Task 0384 added generic optional/default parameter checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 522 cases. Task 0385 rechecked official signals, confirmed the existing `net10.0` MSTest.Sdk/MTP NuGet bridge plus package shards already cover the package-based parallel-test request, kept generated artifacts unchanged, and selected integral numeric shifts next. Task 0386 added integral shift checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 523 cases. Task 0387 rechecked official signals after integral shifts, kept the generated-artifact baseline unchanged, and selected shift assignment expressions next. Task 0388 added shift assignment parser/type-checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 524 cases. Task 0389 rechecked official signals after shift assignments, kept the generated-artifact baseline unchanged, and selected logical unsigned `>>>` expressions next. Task 0390 added logical unsigned shift parser/type-checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 525 cases. Task 0391 rechecked official signals after logical unsigned shifts, kept the generated-artifact baseline unchanged, and selected logical unsigned shift assignment `>>>=` next. Task 0392 added logical unsigned shift assignment parser/type-checker/backend fixtures plus generated `net48` C# consumer evidence, bringing the shared catalog to 526 cases. Task 0393 rechecked official language/platform/package/test/editor/CI signals after local `>>>=`, confirmed the pinned `net10.0` MSTest SDK/MTP package shards already cover the package-based parallel-test route, kept the generated `net48`/C# 7.3 baseline unchanged, and selected imported C# member `>>>=` single-evaluation lowering next. Task 0394 added imported C# instance/static field/property `>>>=` checker/backend coverage with generated `net48` C# consumer evidence and brought the shared catalog to 528 cases. Task 0395 rechecked official language/platform/package/test/editor/CI signals after imported member `>>>=`, confirmed no generated-artifact or test-host package baseline change, and selected imported C# indexer `>>>=` lowering next. Task 0396 added imported C# instance indexer `>>>=` checker/backend coverage with single-evaluation receiver/index-argument generated `net48` C# consumer evidence and brought the shared catalog to 530 cases. Task 0397 rechecked official signals after imported indexer `>>>=`, reaffirmed that the existing `net10.0` `MSTest.Sdk/4.2.3`/MTP package shards already cover the NuGet test-package route while generated artifacts stay package-free `net48`, and selected null-conditional assignment imported member targets next. Task 0398 added `?.` parser coverage plus imported C# instance field/property null-conditional assignment checker/backend coverage with generated `net48` C# consumer evidence and brought the shared catalog to 532 cases. Task 0399 rechecked official signals after null-conditional member assignment, confirmed no baseline/package change, and selected imported C# instance indexer null-conditional assignment next. Task 0400 added `?[]` parser coverage plus imported C# instance indexer null-conditional assignment checker/backend coverage with generated `net48` C# consumer evidence and brought the shared catalog to 534 cases. Task 0402 rechecked official signals after Task 0400, reaffirmed that the existing `net10.0` `MSTest.Sdk/4.2.3`/MTP package shards are already the selected NuGet package path, left CI Task 0401 blocked pending explicit approval for the `npm` process-launch fix, and selected extension member policy as Task 0403. Task 0403 added parser/type-checker/backend/generated `net48` C# consumer evidence for getter-only extension properties and brought the shared catalog to 535 cases with shard expectations `134`, `134`, `134`, and `133`. Task 0404 rechecked official signals after extension properties, confirmed no generated-artifact or test-host package baseline change, reaffirmed the existing MSTest SDK/MTP NuGet package path, and selected extension-property duplicate/conflict diagnostics next. Task 0405 added focused type-checker diagnostics for duplicate/conflicting extension properties without changing the shared catalog count. Task 0406 rechecked official signals and selected helper-name collision diagnostics. Task 0407 added those helper-name collision diagnostics and kept the catalog at 536 cases. Task 0408 rechecked official signals and selected nullable receiver diagnostics. Task 0409 added those nullable receiver diagnostics without changing the shared catalog count. Task 0410 rechecked official signals again, reaffirmed that the existing `net10.0` `MSTest.Sdk/4.2.3`/MTP package bridge and shards already cover the NuGet test-host path, and selected extension property assignment diagnostics next. Task 0412 rechecked official signals after assignment diagnostics, reaffirmed the same baseline and NuGet test-host path, and selected nullable receiver access diagnostics next. Task 0413 added nullable receiver access diagnostics without changing the shared catalog count. Task 0414 rechecked official signals again, confirmed NuGet is already used through the selected `net10.0` MSTest.Sdk/MTP package bridge and shards, and selected extension-property null-conditional access diagnostics next. Task 0415 added null-conditional extension-property read/simple assignment diagnostics without changing the shared catalog count, which remains 536. Task 0416 rechecked official signals again, confirmed no generated-artifact or test-host package drift, kept Task 0401 blocked, and selected imported C# null-conditional member reads next. |
| Regression quality update | Task 0417 added imported C# `receiver?.Member` read coverage for readable metadata-backed instance fields/properties with generated `net48` C# consumer evidence, bringing the shared catalog to 538 cases with shard expectations `135`, `135`, `134`, and `134`. Task 0419 added imported C# `receiver?[index]` read coverage with generated `net48` C# consumer evidence, bringing the shared catalog to 540 cases with shard expectations `135`, `135`, `135`, and `135`. Task 0420 rechecked the package strategy and kept MSTest.Sdk/MTP as the current NuGet package bridge because xUnit.net v3 or NUnit would duplicate the same extracted catalog evidence without improving generated `net48` compatibility. Task 0421 added imported C# `receiver?.Member >>>= count` coverage with generated `net48` C# consumer evidence, bringing the shared catalog to 542 cases with shard expectations `136`, `136`, `135`, and `135`. Task 0422 rechecked the official package/testing signals and again kept the existing `net10.0` `MSTest.Sdk/4.2.3` MTP bridge plus package shards as the selected NuGet path instead of adding a duplicate xUnit.net v3 or NUnit bridge. Task 0423 added imported C# `receiver?[index] >>>= count` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 544 cases with shard expectations `136`, `136`, `136`, and `136`. Task 0424 rechecked official package/testing signals again and kept the existing `net10.0` MSTest.Sdk/MTP package shards as the broad current NuGet test-host path; no catalog-count change. Task 0425 added imported C# `receiver?.Member |= value`, `receiver?.Member &= value`, and `receiver?.Member ^= value` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 546 cases with shard expectations `137`, `137`, `136`, and `136`. Task 0426 rechecked the same package strategy after Task 0425 and again kept MSTest.Sdk/MTP package shards as the current NuGet test-host path while selecting the paired indexer bitwise slice next. Task 0427 added imported C# `receiver?[index] |= value`, `receiver?[index] &= value`, and `receiver?[index] ^= value` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 548 cases with shard expectations `137`, `137`, `137`, and `137`. Task 0428 rechecked official package/testing signals, reaffirmed that TypeSharp already uses NuGet at the `net10.0` test-host boundary through `MSTest.Sdk/4.2.3`, and replaced the CI package shard PowerShell job loop with MTP `--test-modules` module parallelism after a 552-test package shard pass. Task 0429 added imported C# `receiver?.Member += value` and `receiver?.Member -= value` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 550 cases with shard expectations `138`, `138`, `137`, and `137` and package-shard MTP minimum 554. Task 0431 added imported C# `receiver?[index] += value` and `receiver?[index] -= value` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 552 cases with shard expectations `138`, `138`, `138`, and `138` and package-shard MTP minimum 556. Task 0433 added imported C# `receiver?.Member <<= count` and `receiver?.Member >>= count` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 554 cases with shard expectations `139`, `139`, `138`, and `138` and package-shard MTP minimum 558. Task 0435 added imported C# `receiver?[index] <<= count` and `receiver?[index] >>= count` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 556 cases with shard expectations `139`, `139`, `139`, and `139` and package-shard MTP minimum 560. Task 0437 added local `*=`, `/=`, and `%=` parser/type-checker/backend fixtures plus generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 558 cases with shard expectations `140`, `140`, `139`, and `139` and package-shard MTP minimum 562. Task 0439 added imported C# member `*=`, `/=`, and `%=` generated `net48` C# consumer and negative checker coverage, bringing the shared catalog to 560 cases with shard expectations `140`, `140`, `140`, and `140` and package-shard MTP minimum 564. |
| Parallel execution | Task 0259 parallelized source-file parse and per-file semantic validation for compiler `check` and `build`, while preserving deterministic source-order diagnostics and documenting safe parallel work rules. |

## Remaining Known Future Areas

- Non-relative re-export and non-lowerable source re-export forms beyond the current function/literal/value/lambda-valued value/type/module alias forwarding and remapping slice.
- .NET ecosystem follow-ups: compiler NuGet package restore implementation behind lock/source-mapping/audit/license/checksum gates, monitor the 2026-06-08 through 2026-06-15 GitHub Actions Windows Server 2025 + Visual Studio 2026 hosted-runner image migration, Task 0401's pending `npm` process-launch CI fix after explicit approval, a future xUnit.net v3 or NUnit bridge only if it provides distinct ecosystem value beyond the existing `MSTest.Sdk/4.2.3`/MTP package shards, a qualified `net481` profile, VS Code Marketplace publication, `dotnet new` template packs, and host packaging automation.
- TypeScript-derived structural/module follow-ups: implement budgeted advanced type operators in bounded slices and design richer cross-project project-reference re-export metadata.
- Richer match exhaustiveness beyond current known nominal, TypeSharp-owned enum, named imported C# enum, bool, and local type-level union cases, discard arms, guarded-arm non-coverage, and local literal-union cases. Null-conditional compound assignment/invocation/chains/events/static targets/TypeSharp-owned targets beyond the bounded imported member/indexer additive, member/indexer bitwise, member/indexer logical unsigned shift, imported member/indexer regular-shift, simple assignment, and read slices, event `>>>=`, unary boolean complement, user-defined operators, TypeSharp member assignment policy, broad class-member body analysis, flag-style enum algebra beyond same-enum value `|`/`&`/`^`/`~`, broad attribute target validation, arbitrary/general computed enum member expressions beyond enum initializer-local `|`, numeric pattern algebra, flag-style reasoning over imported numeric enum metadata, broader/public composition type inference, higher-order/imported pipeline targets, imported composition targets, pipeline overload ranking, and fuller pattern algebra remain follow-ups.
- Task 0441 should implement imported C# indexer `*=`, `/=`, and `%=` targets for metadata-backed instance indexers with selected public getter/setter pairs using the primitive integral multiplicative assign-back policy. Null-conditional multiplicative compound assignment, floating-point/decimal policy expansion, and user-defined operators remain later slices.
- Fuller indexer conversion/ranking beyond exact/object/null/parenthesized/unary numeric/known numeric/metadata relationship checks, broader contextual generic inference beyond direct TypeSharp simple and bounded constructed generic function, pipeline, params-tail, named-argument, optional/default concrete suffix, and named-unary composition calls, TypeSharp named argument follow-ups beyond bounded direct/pipeline calls and non-`params` generic shapes, optional/default parameter follow-ups beyond concrete trailing literal defaults on direct `fun` declarations, fuller C# overload conversion/contextual ranking beyond current literal/unary numeric/null metadata-specificity/imported metadata relationship/collection expression array argument/single collection expression params-array argument/parenthesized overload argument unwrapping/delegate arity, known return checks, identity lambda parameter return checks, known lambda return conversion ranking, metadata member-chain return inference, metadata instance method-call return inference, imported extension method-call return inference, imported static method-call return inference, comparison/logical binary predicate return inference, string/numeric binary value return inference, `nameof` return inference, checked/unchecked return inference, `satisfies` return inference, parenthesized return inference, unary logical-not return inference, unary numeric return inference, `if` expression return inference, block final-expression return inference, collection expression return inference, null-coalescing return inference, and indexer-expression return inference, richer lambda body contextual typing beyond those known/literal, identity, member-chain, instance/extension/static method-call, binary predicate, binary value, `nameof`, checked/unchecked, `satisfies`, parenthesized, unary logical-not, unary numeric, `if`, block final-expression, collection expression, null-coalescing, and indexer bodies, fuller extension conversion/ranking beyond the implemented duplicate/conflict diagnostics, static extension members, extension setters/operators, nullable extension-property receiver lifting, and imported C# extension property metadata. |
- Future IL backend work after the C# 7.3 source backend remains stable.

## Update Rule

When a long-running task completes:

1. Update `agent/tasks-rollup.md`.
2. Update this page if the current state, theme summary, or remaining future areas changed.
3. Update [Agentic Workflow](../agentic-workflow/) only if the way agents choose or record work changed.
4. Run `npm run build` from `docs`.
