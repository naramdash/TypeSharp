# Diagnostics

문서 기준일: 2026-05-20

이 파일은 task `0251-docs-site-canonical-language-ledger` 이후 남아 있는 short bridge stub다.

Canonical diagnostic code taxonomy, descriptor metadata, explanation surface, text/JSON output shape, and golden diagnostic fixture policy는 [docs-site Diagnostics](../docs-site/src/content/docs/diagnostics.md)다.

## Bridge Scope

- Diagnostic code ranges, allocated descriptor table, descriptor metadata requirements, CLI/LSP output contract, golden diagnostic policy, and current implementation notes는 docs-site Diagnostics로 접혔다.
- CLI usage and exit code rules are in [docs-site CLI](../docs-site/src/content/docs/cli.md).
- 이 bridge는 active agent가 예전 경로를 열었을 때 target canonical page를 찾을 수 있게 하기 위해서만 남아 있다.
