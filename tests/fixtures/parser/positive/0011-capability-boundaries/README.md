# Parser Fixture: Capability Boundaries

Source: `examples/11-capability-boundaries.tysh`

Purpose: parser fixture for explicit `dynamic`, `reflect`, `interop`, and `extern` capability markers, attribute-prefixed native interop declarations, and exported capability-marked functions.
