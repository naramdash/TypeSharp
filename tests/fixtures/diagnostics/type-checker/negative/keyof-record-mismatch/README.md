Purpose: verifies that `keyof` rejects non-member string literals and remains compile-time-only at public boundaries.

Source: `docs-site/src/content/docs/type-system.md` limited `keyof` type operator.
