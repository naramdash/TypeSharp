# Generic Named Argument Lowering

C# backend fixture covering TypeSharp-owned generic direct calls and first-argument pipeline targets whose named arguments are reordered to positional C# calls after type checking.
