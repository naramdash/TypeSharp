# C# Backend Positive: Extension Property Lowering

Purpose: verifies that TypeSharp-authored extension properties lower to C# 7.3-compatible static extension helper methods and member accesses call those helpers.
