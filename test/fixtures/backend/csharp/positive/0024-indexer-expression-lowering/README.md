# C# Backend Positive: Indexer Expression Lowering

Purpose: verifies that array index expressions lower to C# indexer access while preserving the local collection allocation and selected element return.
