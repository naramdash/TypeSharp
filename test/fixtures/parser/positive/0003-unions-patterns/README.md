# Parser Fixture: Unions Patterns

Source: `examples/03-unions-patterns.tysh`

Purpose: parser fixture for named imports, nominal union declarations, generic type parameter/type argument lists, `match` expressions, and union case patterns.
