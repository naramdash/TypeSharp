# Parser Fixture: Pipeline Collections

Source: `examples/07-pipeline-collections.tysh`

Purpose: parser fixture for pipeline expressions, array types, collection literals, exported value declarations, `for` loops, function type annotations, lambdas, named arguments, and fractional decimal literals.
