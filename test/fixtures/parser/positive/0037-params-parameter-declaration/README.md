# Params Parameter Declaration Parser Fixture

Parses TypeSharp-owned `params` parameter declarations on direct function parameter lists so the AST preserves the keyword for checker and backend stages.
