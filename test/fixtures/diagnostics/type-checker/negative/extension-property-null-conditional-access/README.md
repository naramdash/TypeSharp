# Extension property null-conditional access

Purpose: verifies getter-only TypeSharp-authored extension properties report deterministic diagnostics when targeted through null-conditional member access before nullable receiver lifting or TypeSharp-owned null-conditional lowering is implemented.
