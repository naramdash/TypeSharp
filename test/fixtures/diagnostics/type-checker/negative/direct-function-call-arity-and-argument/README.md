# direct-function-call-arity-and-argument

Verifies direct TypeSharp-declared function calls report `TS2215` for known arity mismatches and argument type mismatches while compatible direct and nested calls remain accepted.
