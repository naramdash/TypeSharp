# composition-public-annotation-required

Verifies public direct composition values require explicit function-type annotations so unannotated composition delegate inference stays private/internal only.
