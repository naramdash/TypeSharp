# Composition Function Compatibility

Ensures direct named-function `>>` and `<<` composition reports deterministic type-checker diagnostics when a known unary function return type cannot flow into the next function's parameter type.
