# Optional/default parameter declaration negative fixture

Validates deterministic diagnostics for non-trailing defaults, `params` interaction, unsupported default expressions, mismatched defaults, generic exclusions, explicit-type requirements, ambient/`extern` exclusions, and arity failures.
