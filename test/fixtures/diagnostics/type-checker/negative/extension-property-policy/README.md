# Type Checker Negative: Extension Property Policy

Purpose: verifies that extension properties require a declaration receiver name, immutable getter-only value syntax, an explicit type annotation, and an initializer expression.
