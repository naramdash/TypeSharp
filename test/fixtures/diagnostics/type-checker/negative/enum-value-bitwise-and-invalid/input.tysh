namespace Samples.TypeChecker.EnumValueBitwiseAndInvalid

enum Permission {
  Read,
  Write
}

enum Color {
  Red,
  Blue
}

let mixedEnum: Permission = Permission.Read & Color.Red

let numericAnd = 1 & 2

let enumAndNumber: Permission = Permission.Read & 1
