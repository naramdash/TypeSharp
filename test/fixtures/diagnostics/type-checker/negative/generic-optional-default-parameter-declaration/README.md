# Generic Optional Default Parameter Declaration Diagnostics

Type checker fixture covering generic optional/default exclusions where a defaulted parameter type directly or transitively references the function type parameter.
