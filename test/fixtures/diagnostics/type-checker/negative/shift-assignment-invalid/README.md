# Shift Assignment Invalid Type Checker Fixture

Purpose: verifies deterministic diagnostics for invalid `<<=` and `>>=` targets, nullable operands, non-integral operands, unsupported counts, and immutable locals.
