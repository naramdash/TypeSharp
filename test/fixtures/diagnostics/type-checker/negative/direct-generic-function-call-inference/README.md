# direct-generic-function-call-inference

Verifies direct TypeSharp-declared generic function calls report `TS2201` for explicit generic arity mismatches, simple and constructed argument mismatches, repeated type-parameter inference conflicts, and substituted return type mismatches.
