# direct-generic-function-call-inference

Verifies direct TypeSharp-declared generic function calls report `TS2215` for explicit generic arity mismatches, simple and constructed argument mismatches, and repeated type-parameter inference conflicts, while substituted return value initializer mismatches report `TS2229`.
