# Multiplicative Compound Assignment Type Checker Fixture

Purpose: validates bounded `*=`, `/=`, and `%=` assignments for mutable primitive integral, floating-point, and decimal local targets, including local `checked(...)` and `unchecked(...)` wrappers.
