# direct-generic-composition-inference

Verifies direct named-function composition accepts bounded TypeSharp generic unary targets when the composition edge can infer simple, array, and matching single-argument wrapper type parameters.
