# direct-generic-function-call-inference

Verifies direct TypeSharp-declared generic function calls infer or accept simple generic type arguments and return substituted result types without diagnostics.
