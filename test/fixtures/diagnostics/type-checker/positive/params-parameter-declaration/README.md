# Params Parameter Declaration Positive Fixture

Checks final array `params` parameters on TypeSharp-declared functions, including exact array calls, expanded calls, pipeline lowering arity, and bounded generic inference.
