namespace Samples.TypeChecker.EnumDeclaration

enum Color : byte {
  Red = 1,
  Crimson = Red,
  Green,
  Blue = 4,
  Purple = Red | Blue
}

let favorite: Color = Color.Red | Color.Blue

fun choose(): Color =
  Color.Red | Color.Blue
